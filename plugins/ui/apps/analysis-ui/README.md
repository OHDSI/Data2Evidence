# Analysis Dataflow UI

To develop locally, these are the following steps:

- Run `bun nx dev analysis-ui` in `./ui`, this will serve module.js in https://localhost:8083/
- Run `bun nx start vue-mri` in `./ui` for running PA locally.
- Open https://localhost:8081 to access Portal and navigate to Analysis to access analysis flow.
