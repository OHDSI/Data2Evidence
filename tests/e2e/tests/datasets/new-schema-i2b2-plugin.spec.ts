import { test, expect } from '@playwright/test';

test('dataset-new-schema-i2b2-plugin', async ({ page }) => {
  await page.goto('https://localhost:443/portal', { waitUntil: 'networkidle' });
  await page.waitForSelector('input[name="identifier"]', { timeout: 30000 });
  await page.locator('input[name="identifier"]').click();
  await page.waitForSelector('input[name="identifier"]', { timeout: 30000 });
  await page.locator('input[name="identifier"]').fill('admin');
  await page.waitForSelector('input[name="password"]', { timeout: 30000 });
  await page.locator('input[name="password"]').click();
  await page.waitForSelector('input[name="password"]', { timeout: 30000 });
  await page.locator('input[name="password"]').fill('Updatepassword12345');
  await page.waitForSelector('button:has-text("Sign in")', { timeout: 30000 });
  await page.getByRole('button', { name: 'Sign in' }).click();
  await page.waitForSelector('[data-testid="button"]', { timeout: 30000 });
  await page.getByTestId('button').nth(1).click();
  await page.waitForSelector('button:has-text("Switch to Admin portal")', { timeout: 30000 });
  await page.getByRole('button', { name: 'Switch to Admin portal' }).click();
  await page.waitForSelector('a:has-text("Datasets")', { timeout: 30000 });
  await page.getByRole('link', { name: 'Datasets' }).click();
  await page.waitForSelector('button:has-text("Add dataset")', { timeout: 30000 });
  await page.getByRole('button', { name: 'Add dataset' }).click();
  await page.waitForSelector('input[aria-label*="Dataset name"]', { timeout: 30000 });
  await page.getByRole('textbox', { name: 'Dataset name - Displayed on' }).click();
  await page.waitForSelector('input[aria-label*="Dataset name"]', { timeout: 30000 });
  await page.getByRole('textbox', { name: 'Dataset name - Displayed on' }).fill('Test Study 5');
  await page.waitForSelector('input[aria-label*="Dataset summary"]', { timeout: 30000 });
  await page.getByRole('textbox', { name: 'Dataset summary' }).click();
  await page.waitForSelector('input[aria-label*="Dataset summary"]', { timeout: 30000 });
  await page.getByRole('textbox', { name: 'Dataset summary' }).fill('Test Summary');
  await page.waitForSelector('pre', { timeout: 30000 });
  await page.locator('pre').nth(1).click();
  await page.waitForSelector('#simplemde-editor-1-wrapper [role="textbox"]', { timeout: 30000 });
  await page.locator('#simplemde-editor-1-wrapper').getByRole('textbox').fill('Test Description');
  await page.waitForSelector('[data-testid="dialog"] div:has-text("CDM Schema Option")', { timeout: 30000 });
  await page.getByTestId('dialog').locator('div').filter({ hasText: 'CDM Schema Option' }).nth(4).click();
  await page.waitForSelector('li[role="option"]:has-text("Create new schema")', { timeout: 30000 });
  await page.getByRole('option', { name: 'Create new schema', exact: true }).click();
  await page.waitForSelector('#mui-component-select-databaseOption', { timeout: 30000 });
  await page.locator('#mui-component-select-databaseOption').click();
  await page.waitForSelector('li[role="option"]:has-text("demo_database-postgres")', { timeout: 30000 });
  await page.getByRole('option', { name: 'demo_database-postgres' }).click();
  await page.waitForSelector('#mui-component-select-vocabSchemaOption', { timeout: 30000 });
  await page.locator('#mui-component-select-vocabSchemaOption').click();
  await page.waitForSelector('li[role="option"]:has-text("demo_cdm")', { timeout: 30000 });
  await page.getByRole('option', { name: 'demo_cdm' }).click();
  await page.waitForSelector('#mui-component-select-dataModelOption', { timeout: 30000 });
  await page.locator('#mui-component-select-dataModelOption').click();
  await page.waitForSelector('li[role="option"]:has-text("v1.8.1 [i2b2_plugin]")', { timeout: 30000 });
  await page.getByRole('option', { name: 'v1.8.1 [i2b2_plugin]' }).click();
  await page.waitForSelector('#mui-component-select-paConfigOption', { timeout: 30000 });
  await page.locator('#mui-component-select-paConfigOption').click();
  await page.waitForSelector('li[role="option"]:has-text("I2B2")', { timeout: 30000 });
  await page.getByRole('option', { name: 'I2B2', exact: true }).click();
  await page.waitForSelector('input[aria-label*="Token dataset code"]', { timeout: 30000 });
  await page.getByRole('textbox', { name: 'Token dataset code' }).click();
  await page.waitForSelector('input[aria-label*="Token dataset code"]', { timeout: 30000 });
  await page.getByRole('textbox', { name: 'Token dataset code' }).fill('ts5');
  await page.waitForSelector('button:has-text("Add")', { timeout: 30000 });
  await page.getByRole('button', { name: 'Add', exact: true }).click();
  await page.waitForSelector('text=Test Study 5', { timeout: 30000 });
  await expect(page.getByText('Test Study 5')).toBeVisible();
  await page.waitForSelector('a:has-text("Jobs")', { timeout: 30000 });
  await page.getByRole('link', { name: 'Jobs' }).click();
  await page.waitForSelector('a:has(span:text("datamodel-create-cdm_ts5_"))', { timeout: 30000 });
  // Get the first (top) entry link
  const firstEntry = page.locator('a:has(span:text("datamodel-create-cdm_ts5_"))').first();
  // Find the closest state badge to this entry (adjust the selector as needed)
  const stateBadge = firstEntry.locator('xpath=ancestor::div[contains(@class,"state-list-item__content")]//span[contains(@class,"state-badge")]');
  await expect(stateBadge).toHaveText(/Completed/, { timeout: 120000 });  
});