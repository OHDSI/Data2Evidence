# Build context filter for Dockerfile.v2. The context is services/trex, which
# holds only d2e's own config, plugins and scripts — the trex source tree that
# the long Trex/** exclusion list used to guard against was removed when this
# image moved onto the published trexsql base.
**/node_modules
**/target
**/build
**/.git
