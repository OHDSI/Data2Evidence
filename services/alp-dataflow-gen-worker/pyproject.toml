[project]
name = "d2e-dataflow-worker"
version = "0.1.0"
description = "Prefect process worker for D2E flow plugins"
requires-python = "==3.12.*"
dependencies = ["prefect[docker]==3.6.10"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["linux-64"]

[tool.pixi.dependencies]
python = "3.12.*"
