# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# the repo root ignores all package-lock.json; the website needs its lockfile
# committed for reproducible builds and the CI npm cache
!package-lock.json
